# Configuración para un servidor backend
server-type: "backend"

debug:
  # Si es 'true', se mostrarán los mensajes de [DEBUG-Heartbeat] en la consola.
  # Si es 'false', la consola estará limpia.
  heartbeat: false